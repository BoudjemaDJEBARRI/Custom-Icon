import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.contactsApp',
  appName: 'icon-custom',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
